import { DirectiveHome1Directive } from './directive-home1.directive';

describe('DirectiveHome1Directive', () => {
  it('should create an instance', () => {
    const directive = new DirectiveHome1Directive();
    expect(directive).toBeTruthy();
  });
});
